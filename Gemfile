source "https://rubygems.org"

# GitHub Pages builds this site natively, so no CI workflow is required.
# The site uses only core Jekyll features and no plugins, which keeps it
# buildable by both the GitHub Pages toolchain and a current local Jekyll.
gem "jekyll", "~> 4.3"

gem "webrick", "~> 1.8"
